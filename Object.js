/*const user = {
    name: "vijay",
    age:41,
    greet(){
        console.log(`Hi, I am ${this.name}`);
    }
};

const colors = ['red','green','blue'];

colors.forEach(color=>console.log(color));

const lengths=colors.map(color=> color.length);
console.log(lengths);
console.log(user); 
*/

const beta = [9,10,11,];
beta.forEach((x) => console.log(x+8));

const gamma = beta.map((x)=>x*2);
console.log(gamma);